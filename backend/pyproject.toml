[project]
name = "steamquery-backend"
version = "0.1.0"
description = "Backend API for SteamQuery"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "pydantic>=2.12.5",
    "ruff>=0.15.5",
]
